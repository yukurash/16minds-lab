# RULES — 実験手順の事前登録

作成日: 2026-08-21（実験開始前）

このファイルは**実験を回す前に**固定する。ここに書いた基準は、パイロットで一度だけ見直す
機会があり、そこで凍結する。凍結後に基準を変えた場合は、変更点と理由をこのファイルの末尾に
追記し、変更前の結果も残す。

---

## 何を測るか

16minds-plugin の 16 人格について、次の 2 枚の表を実測で作る。

1. **適性表** — 16 タイプ × 8 タスク。どのタイプがどの仕事で使える指摘を出すか
2. **相性表** — 16C2 = 120 ペア総当たり。どのペアの議論が噛み合うか

得られた表を `casting.json` にまとめ、プラグイン側の `/cast`（お題に応じた自動キャスティング）
と `--rival`（最良の論敵の自動選出）に使う。

## 何を測らないか

- 人格の演じ分けが「本物の MBTI として」正しいかどうかは測らない。これは診断ではない
- モデルの絶対性能は測らない。同一モデル上でプロンプトだけが違う 16 条件の比較である

---

## 被験体

`yukurash/16minds-plugin` の `agents/<type>.md` を system prompt にそのまま使う。
前処理は次の 2 点だけ。それ以外は 1 文字も書き換えない（`harness/personas.py`）。

1. YAML frontmatter（`name` / `description`）を落とす
   — Claude Code のサブエージェント登録用メタデータであり、人格の記述ではないため
2. 末尾の「出力フォーマットは呼び出し元のコマンドが指定する。…」の節を落とす
   — `/mind` 用のフォーマット指示であり、本実験では `--json-schema` で上書きするため

## 実行環境

```
claude -p --system-prompt <persona> --model <model>
        --tools "" --safe-mode --strict-mcp-config
        --no-session-persistence --output-format json --json-schema <schema>
```

- `--safe-mode` で CLAUDE.md・skills・plugins・hooks・MCP をすべて無効化する
- `--tools ""` でツール使用を封じる（純粋な文章生成にする）
- お題本文はコマンドライン長を超えるため stdin から渡す
- 認証はサブスクリプション（API キーは使わない）
- モデル: 本番は `sonnet` で全数。`spec_hole` タスクのみ `opus` で 16 タイプ再実行し、
  順位が反転しないかを確認する（スロット名 `opus`）
- Claude Code のバージョンは実行時に `results/env.json` に記録する

## 題材

`yukurash/yukurash.github.io` の `index.html`（単一ファイル 799 行）。
レビュー能力を測るための欠陥を意図的に 4 件仕込んだスナップショットを入力とする。

- ブランチ: `experiment/16minds-fixtures`
- 分岐元: `main` @ `2af106659d19c885a6e363f85d8452e7026866a1`
- 固定コミット: `9c63b3c19a28352c1b375750360362cb033db012`
- Draft PR: https://github.com/yukurash/yukurash.github.io/pull/6 （**マージしない**）

### 封印

仕込み欠陥の内容と正解リストは、実験が終わるまで公開しない。ハッシュだけ先に出す。

| ファイル | SHA-256 |
|---|---|
| `fixtures/index.html` | `78cf9f140184ee9868b0bf885afed93652da5a1cf80856807f3dd57f45ffdde3` |
| `fixtures/ANSWERS.md` | `27c15904a2fe18979f1e52c72544454fd4ac8787f75f60063771eb0135fb7395` |

`ANSWERS.md` は `.gitignore` に入れてあり、実験完了時に ignore を外して公開する。
そのときこのハッシュと一致することを確認できる。

---

## 実験1：適性表

- 16 タイプ × 8 タスク × 3 反復 = 384 セル
- 8 タスク: `bug` / `spec_hole` / `design_review` / `naming` / `estimate` / `incident` /
  `refactor` / `tech_choice`（定義は `harness/tasks_fit.py`）
- 全タスクで出力スキーマは共通。指摘は最大 5 件

### 採点

タスク × 反復ごとに 16 出力を**匿名化し、固定シードでシャッフルして**採点者に一括で渡す。
採点者は型名を知らず、人格も持たない。ラベルと型の対応は `f"{slot}/{task}/{rep}"` を
シードにした `random.Random` で決まるので、後から再現できる。

| 指標 | 定義 | 対象 |
|---|---|---|
| **検出率**（主指標） | 正解リストの項目をいくつ当てたか | `bug` `spec_hole` `incident` |
| 有用性 | 0-5。そのまま着手できる指摘か | 全タスク |
| 具体性 | 0-5。行番号・セレクタ名で位置が特定できているか | 全タスク |
| 独自性 | 16 人中その 1 人しか挙げていない論点の数 | 全タスク |
| 順位 | そのタスク内での総合順位 1-16。重複禁止 | 全タスク |

ヒートマップに載せる値は**順位を 0-1 に正規化したもの**とする。検出率は尺度がタスクごとに
違うため、`results/detection_<slot>.md` に別表で出す。

主指標を客観指標のあるタスクに置き、採点者の主観に全依存させない。

---

## 実験2：相性表

- 120 ペア総当たり。お題は `portfolio_failures`（`harness/tasks_pair.py`）
- `/pair heavy` と同じ 3 フェーズ（立論 → 反論 → 再反論）
- Phase 3 冒頭の「修正なし / 部分修正」の宣言は既存コマンドの仕様であり、これをそのまま
  立場修正の指標として使う

### 呼び出し回数

立論は相手に依存せずお題だけに依存するため、**16 タイプぶん 1 回だけ生成して全ペアで
使い回す**。変数統制としても正しい（同じ立論を、ぶつける相手だけ変える）。

| フェーズ | コール数 |
|---|---|
| 立論 | 16 |
| 反論（双方向） | 240 |
| 再反論（双方向） | 240 |
| 採点 | 120 |

### 採点

| 指標 | 定義 |
|---|---|
| **新規論点数**（主指標） | 両者の立論のどちらにも無かった論点が、反論・再反論で新しくいくつ生まれたか |
| 組ませた価値 | 0-5。片方だけでは出なかった結論に届いたか。新規論点数の同点破りに使う |
| 論点接触率 | 0-3。その人の反論が相手の実際の主張に触れていたか |
| 立場修正 | Phase 3 の宣言（修正なし / 部分修正） |
| 平行線 | 最後まで評価軸が一度も交差しなかったか |

### 追加で確認すること

- **順序効果**: 代表 10 ペアを先攻後攻を入れ替えて再実行し、結果が変わるかを見る
- **お題依存**: 代表 20 ペアを別のお題 `article_axis` で再実行し、相性表がお題に
  引きずられていないかを見る

---

## 段階

1. パイロット: 適性 1 反復ぶんの 2 タスク、相性 4 タイプ 6 ペア
2. 採点基準がパイロットで機能するかを確認し、**ここで基準を凍結**する
3. 全数

---

## 無効ランの扱い

次のいずれかに当たったセルは無効とし、再実行する。再実行後も同じなら欠測として扱い、
記事に件数を明記する。

- `claude -p` が `is_error` を返した（認証・レート制限・API エラー）
- 出力が `--json-schema` の検証を通らなかった
- タイムアウト（既定 300 秒）
- `findings` が 0 件

キャッシュが効いているため、再実行しても成功済みのセルは呼び直さない。

## 免責（記事に必ず書く）

- 自作ベンチマークであり、標準的なベンチではない
- 反復は 3 回、ペアは 1 回。統計的に有意な差を主張できる規模ではない
- 同一モデル上の 16 条件の比較であって、人間の MBTI について何かを言うものではない
- 採点者も LLM である。基準・プロンプト・シャッフルのシードはすべてこのリポジトリにある

---

## 変更履歴

- 2026-08-21: 初版（実験開始前）
- 2026-08-21: **パイロット後の凍結**。基準の見直しはここまでとし、以降は変更しない。

  パイロット（`bug` / `spec_hole` を 1 反復、32 セル、失敗 0）で、採点の解像度が
  足りないことが分かったため次を変更した。生成側は一切変えていないので、パイロットの
  32 セルはそのまま採点し直して使っている。

  - 有用性・具体性を 0-3 から **0-5** に広げ、各点にアンカーとなる説明文を付けた
    （0-3 では 16 人中 14 人が同点になり、差がまったく出なかった）
  - タスク内の**総合順位（1-16、重複禁止）**を採点項目に追加した
  - ヒートマップの値を、正規化した順位に変更した

  順位は重複を禁じているため、実質的に差のない出力にも強制的に順序がつく。
  1 反復ぶんの順位は信用しない。3 反復の平均で見る前提の指標である。これは記事にも書く。

  変更しなかったもの: 指摘の上限 5 件（優先順位づけ自体が測りたい能力なので据え置き）、
  正解リスト、題材、モデル、無効ランの定義。

- 2026-08-21: **相性側のパイロットを受けた修正**。適性側と同じ「指標が飽和した箇所を
  直す」ための変更で、これをもって相性側も凍結する。

  パイロット（4 タイプ 6 ペア、失敗 0）で次の 2 点が判明した。

  1. 再反論の宣言が **12 人中 12 人が「部分修正」** で、まったく散らばらなかった。
     原因はこちらのプロンプトで、「相手の反論に一理あると思ったなら、そう認めたうえで
     部分修正しろ。見栄を張るな」と書いていた。測ろうとしている結果を指示文が先に
     決めてしまっている。この一文を、どちらの宣言も等価に扱う中立な言い回しに直した。
  2. `parallel` が 6 ペアすべて False で飽和した。指標としては残すが、主指標には
     しない（結果は記事に「飽和した」と書く）。
  3. 新規論点数が 3〜5 の範囲に収まり、同点が多く出ることが予想されたため、
     同点破りとして **組ませた価値 0-5** を採点項目に追加した。

  立論と反論のプロンプトは変えていないので、生成済みのその 2 フェーズは再利用する。
  再反論だけ 6 ペアぶん取り直した。
